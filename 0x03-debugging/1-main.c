#include <stdioh>

/**
 * main - causes an infinite loop
 * Return: 0
 */

int main(void)
{
	int i;

	printf("Infinite loop incoming :(\n");

	i = 0;
	/**
	 * while (i < 10)
	 * {
	 *putchar(i);
	 *}
	 */
	printf("%d is the largest number\n", largest);

	return (0);
}
