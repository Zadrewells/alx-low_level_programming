#include "main.h"

/**
 * mul - function prototype
 * Description: Taskes two integers as arguments and returns their product.
 * @a: The first number to multiply
 * @b: The second number to multiply
 * Return: The product of a and b
 */
int mul(int a, int b)
{
	int p = a * b;

	return (p);
}
